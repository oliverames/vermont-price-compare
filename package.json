{
  "name": "vermont-price-compare",
  "version": "1.0.0",
  "private": true,
  "description": "A static catalogue for comparing published Vermont healthcare procedure prices.",
  "scripts": {
    "test": "npm run test:js && npm run test:python",
    "test:js": "node --test tests/*.test.js",
    "test:python": "/opt/homebrew/bin/python3 -m unittest discover -s tests -p 'test_*.py'",
    "check": "node --check app.js && node --check search-worker.js && node --check access-gate.js && npm test"
  }
}
